path = require \path
cluster = require \cluster
os = require \os
require \sugar

Object.extend!

exports.Gusto = {}
exports.config = new
	@appDir = process.cwd!
	@routes = path.join @appDir,"conf","routes.conf"

require "./server/server.co"
if cluster.isMaster
	cluster.fork! for p of os.cpus!
else (new exports.Gusto.Server).go 8001