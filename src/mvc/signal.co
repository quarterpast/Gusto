class exports.signal
	$callbacks: []
	$queue: []
	(def)~>
		@$callbacks[0] = def if def?
	connect: (cb)->
		cb.apply this,@$queue.shift! if @$queue.length
		@$callbacks.push cb
	fire: ->
		for cb of @$callbacks
			cb ... 
		else @$queue.push arguments
