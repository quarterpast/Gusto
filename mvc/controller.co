class exports.Controller
	@register: (@result,@base,@action)->
	@subclasses = []
	@extended = -> 
		it.id = [it.displayName]
		@subclasses.push it
	renderJSON: ->
		..result.writeHead 200, \content-type: "application/json"
		..result.end JSON.stringify it
	